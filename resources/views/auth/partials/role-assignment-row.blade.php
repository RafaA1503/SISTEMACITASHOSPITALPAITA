<article class="user-admin-card role-assignment {{ $item->active ? '' : 'is-inactive' }}" data-row-id="{{ $item->id }}">
 <div class="user-identity"><div class="avatar">{{ mb_strtoupper(mb_substr($item->name,0,2,'UTF-8'),'UTF-8') }}</div><div><strong>{{ $item->name }}</strong><small>{{ $item->email }}</small><em class="account-state {{ $item->active ? 'active' : 'inactive' }}">{{ $item->active ? 'Acceso activo' : 'Acceso suspendido' }}</em></div></div>
 <form method="POST" action="{{ route('admin.roles.assign',$item) }}" class="ajax-form role-select-form">@csrf @method('PUT')<label>Rol personalizado<select name="custom_role_id" required @disabled($roles->isEmpty())><option value="" disabled @selected(!$roles->contains('idRol', $item->idRol))>{{ $roles->isEmpty() ? 'Crea primero un rol' : 'Seleccionar rol' }}</option>@foreach($roles as $custom)<option value="{{ $custom->id }}" @selected($item->idRol===$custom->id)>{{ $custom->name }}</option>@endforeach</select><small>El rol define las acciones disponibles.</small></label><button @disabled($roles->isEmpty())><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>Guardar rol</button></form>
 <form method="POST" action="{{ route('admin.roles.users.status',$item) }}" class="ajax-form user-status-form" data-confirm="{{ $item->active ? '¿Dar de baja a este usuario? Perderá el acceso, pero sus datos se conservarán.' : '' }}">@csrf @method('PUT')<input type="hidden" name="active" value="{{ $item->active ? '0' : '1' }}"><button type="submit" class="{{ $item->active ? 'danger-action' : 'restore-action' }}">@if($item->active)<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="7" y1="4" x2="7" y2="20"></line><line x1="17" y1="4" x2="17" y2="20"></line></svg>Dar de baja @else <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="1 4 1 10 7 10"></polyline><path d="M3.51 15a9 9 0 1 0 2.13-9.36L1 10"></path></svg>Reactivar @endif</button></form>
 <form method="POST" action="{{ route('admin.roles.users.destroy',$item) }}" class="ajax-form user-delete-form" data-confirm="¿Eliminar permanentemente a {{ $item->name }}? Esta acción no se puede deshacer.">@csrf @method('DELETE')<button type="submit" class="role-delete"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6l-1.5 14a2 2 0 0 1-2 1.8h-7a2 2 0 0 1-2-1.8L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path><path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"></path></svg>Eliminar usuario</button></form>
 <form method="POST" action="{{ route('admin.roles.users.password',$item) }}" class="ajax-form password-reset-form" data-confirm="¿Cambiar la contraseña de {{ $item->name }}?"><input type="hidden" name="_method" value="PUT">@csrf<label>Nueva contraseña<input type="password" name="password" required minlength="8" autocomplete="new-password" placeholder="Mínimo 8 caracteres"></label><label>Confirmar contraseña<input type="password" name="password_confirmation" required minlength="8" autocomplete="new-password" placeholder="Repite la contraseña"></label><button type="submit">Cambiar clave</button></form>
</article>
