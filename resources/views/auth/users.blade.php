<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Usuarios | Hospital Nuestra Señora de las Mercedes</title><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Manrope:wght@700;800&display=swap" rel="stylesheet">@vite(['resources/css/app.css','resources/js/app.js'])</head><body><div class="settings-page"><header><a href="{{ route('portal','administrador') }}">← Volver al portal</a><strong>Administración de usuarios</strong><a href="{{ route('account.settings') }}">Mi cuenta</a></header><main><div class="admin-heading"><div><p>SEGURIDAD Y ACCESO</p><h1>Usuarios, roles y funciones</h1><span>Los cambios se aplican en el siguiente acceso del usuario.</span></div><b>{{ $users->count() }} usuarios</b></div>@if(session('success'))<div class="success-banner">✓ {{ session('success') }}</div>@endif<div class="user-admin-list">@foreach($users as $item)<form method="POST" action="{{ route('admin.users.update',$item) }}" class="user-admin-card">@csrf @method('PUT')<div class="user-identity"><div class="avatar">{{ mb_strtoupper(mb_substr($item->name,0,2,'UTF-8'),'UTF-8') }}</div><div><strong>{{ $item->name }}</strong><small>{{ $item->email }}</small></div></div><label>Rol<select name="role">@foreach(['administrador','portero','admision','profesional','laboratorio','imagenes'] as $role)<option value="{{ $role }}" @selected($item->role===$role)>{{ ucfirst($role) }}</option>@endforeach</select></label><label>Servicio<select name="service_id"><option value="">Sin servicio</option>@foreach($services as $service)<option value="{{ $service->id }}" @selected($item->service_id===$service->id)>{{ $service->name }}</option>@endforeach</select></label><fieldset><legend>Funciones</legend>@foreach(['buscar_pacientes'=>'Buscar pacientes','gestionar_citas'=>'Gestionar citas','crear_ordenes'=>'Crear órdenes','registrar_resultados'=>'Registrar resultados'] as $value=>$label)<label><input type="checkbox" name="permissions[]" value="{{ $value }}" @checked(in_array($value,$item->permissions??[]))> {{ $label }}</label>@endforeach</fieldset><label class="active-user"><input type="checkbox" name="active" value="1" @checked($item->active)> Usuario activo</label><button>Guardar cambios</button></form>@endforeach</div></main></div></body></html>
