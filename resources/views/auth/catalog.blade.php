<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Servicios y profesionales | Hospital La Merced</title><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Manrope:wght@700;800&display=swap" rel="stylesheet">@vite(['resources/css/app.css','resources/js/app.js'])</head><body><div class="settings-page"><header><a href="{{ route('portal','administrador') }}">← Volver</a><strong>Servicios y profesionales</strong><a href="{{ route('admin.roles') }}">Roles</a></header><main>@if(session('success'))<div class="success-banner">✓ {{ session('success') }}</div>@endif @if($errors->any())<div class="auth-error">{{ $errors->first() }}</div>@endif<div class="admin-heading"><div><p>CONFIGURACIÓN CLÍNICA</p><h1>Servicios y profesionales</h1><span>Cada profesional verá únicamente los pacientes enviados a su servicio.</span></div></div><div class="account-grid"><section class="account-card"><h2>Crear servicio</h2><p>Se generará automáticamente una atención de consulta para agendar pacientes.</p><form method="POST" action="{{ route('admin.services.store') }}">@csrf<label>Nombre del servicio<input name="name" required placeholder="Ej. Cardiología"></label><label>Código<input name="code" required placeholder="CARD"></label><label>Ubicación<input name="location" placeholder="Piso 1 · Consultorio 108"></label><button class="login-primary">Crear servicio</button></form></section><section class="account-card"><h2>Crear profesional</h2><p>Crea sus credenciales y asígnalo al servicio correspondiente.</p><form method="POST" action="{{ route('admin.professionals.store') }}">@csrf<label>Nombre completo<input name="name" required placeholder="Dr. Nombre Apellido"></label><label>Correo institucional<input name="email" type="email" required></label><label>Contraseña inicial<input name="password" type="password" minlength="8" required></label><label>Servicio<select name="service_id" required><option value="">Seleccionar</option>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }}</option>@endforeach</select></label><button class="login-primary">Crear profesional</button></form></section></div><div class="admin-heading catalog-heading"><div><h1>Configuración actual</h1></div></div><section class="portal-panel"><div class="admin-services">@foreach($services as $service)<article><span>✚</span><div><h3>{{ $service->name }}</h3><p>{{ $service->location }} · {{ $service->appointment_types_count }} atención(es)</p></div><b>{{ $service->code }}</b></article>@endforeach</div></section><div class="user-admin-list catalog-professionals">@foreach($professionals as $professional)<div class="user-admin-card"><div class="user-identity"><div class="avatar">{{ strtoupper(substr($professional->name,0,2)) }}</div><div><strong>{{ $professional->name }}</strong><small>{{ $professional->email }}</small></div></div><span>{{ $professional->service?->name }}</span><span>{{ $professional->active?'Activo':'Inactivo' }}</span></div>@endforeach</div></main></div></body></html>
