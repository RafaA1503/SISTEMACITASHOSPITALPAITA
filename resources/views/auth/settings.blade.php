<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>Mi cuenta | Hospital La Merced Paita</title><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Manrope:wght@700;800&display=swap" rel="stylesheet">@vite(['resources/css/app.css','resources/js/app.js'])</head><body><div class="settings-page"><header><a href="{{ route('portal',$user->role) }}">← Volver al portal</a><strong>Configuración de cuenta</strong><form method="POST" action="{{ route('logout') }}">@csrf<button>Cerrar sesión</button></form></header><main>@if(session('success'))<div class="success-banner">✓ {{ session('success') }}</div>@endif<section class="account-intro"><div class="profile-photo">@if($user->photo_path)<img src="{{ asset('storage/'.$user->photo_path) }}" alt="Foto de perfil">@else{{ strtoupper(substr($user->name,0,2)) }}@endif</div><div><p>{{ strtoupper($user->role) }}</p><h1>{{ $user->name }}</h1><span>{{ $user->email }}</span></div></section><div class="account-grid"><section class="account-card"><h2>Perfil y fotografía</h2><p>Actualiza la información visible de tu cuenta.</p><form method="POST" action="{{ route('profile.update') }}" enctype="multipart/form-data">@csrf @method('PUT')<label>Nombre completo<input name="name" value="{{ $user->name }}" required></label><label>Fotografía<input name="photo" type="file" accept="image/png,image/jpeg"></label><button class="login-primary">Guardar perfil</button></form></section><section class="account-card"><h2>Cambiar contraseña</h2><p>Utiliza una contraseña segura de al menos 8 caracteres.</p><form method="POST" action="{{ route('password.update') }}">@csrf @method('PUT')<label>Contraseña actual<input name="current_password" type="password" required></label><label>Nueva contraseña<input name="password" type="password" required></label><label>Confirmar nueva contraseña<input name="password_confirmation" type="password" required></label><button class="login-primary">Cambiar contraseña</button></form></section><section class="account-card passkey-card"><h2>Huella digital y Passkeys</h2><p>Configura la biometría del teléfono, Face ID o Windows Hello.</p><div class="passkey-state"><span>◎</span><div><strong>{{ $passkeys->count() }} dispositivo(s) configurado(s)</strong><small>Las huellas nunca se guardan en el hospital.</small></div></div><button class="passkey-register" id="passkeyRegister">＋ Configurar huella en este dispositivo</button>@foreach($passkeys as $passkey)<div class="saved-passkey"><span>◎</span><div><strong>{{ $passkey->name }}</strong><small>Registrada {{ $passkey->created_at->format('d/m/Y') }}</small></div></div>@endforeach</section>@if($user->role==='administrador')<section class="account-card admin-link"><h2>Administración de usuarios</h2><p>Asigna roles, servicios, funciones y estados de acceso.</p><a href="{{ route('admin.users') }}">Gestionar usuarios →</a></section>@endif</div></main></div></body></html>
