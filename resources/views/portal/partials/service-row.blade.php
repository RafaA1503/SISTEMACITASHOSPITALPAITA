<tr data-row-id="{{ $a->id }}"><td><strong>{{ $a->scheduled_at->format('H:i') }}</strong></td><td><strong>{{ $a->patient->full_name }}</strong><small>{{ $a->patient->dni }}</small></td><td>{{ $a->type->name }}<small>{{ $a->type->service->name }}</small></td><td>{{ $a->professional?->name ?? 'Por asignar' }}</td><td><span class="status {{ $a->status }}">{{ ucfirst(str_replace('_',' ',$a->status)) }}</span></td><td>@if(in_array($a->status,['confirmada','ingreso']))<form method="POST" action="{{ route('appointments.complete',$a) }}" class="ajax-form" data-list-container="#serviceTableBody">@csrf @method('PUT')<button class="confirm-small">Confirmar atención</button></form>@elseif($a->status==='atendida')<span class="confirmed-label">✓ Atendida</span>@else<span class="waiting-label">Esperando a Portería</span>@endif</td></tr>
